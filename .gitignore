node_modules/
.output/
.vite/
dist/
.DS_Store
.tanstack/
*.log
coverage/
.env
.env.local
.env.*.local
tsconfig.tsbuildinfo
playwright-report/
test-results/

# CI workflow file is checked-in locally but NOT in the initial
# push (the GitHub token used to seed the repo lacked the `workflow`
# scope). Add it via:
#   git add -f .github/workflows/ci.yml
#   git commit -m "ci: add GitHub Actions workflow"
# once the token is rotated.
.github/workflows/
